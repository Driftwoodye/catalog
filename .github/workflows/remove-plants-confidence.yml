# Temporary cleanup workflow disabled after successful plants.csv cleanup.
name: Disabled cleanup placeholder
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
